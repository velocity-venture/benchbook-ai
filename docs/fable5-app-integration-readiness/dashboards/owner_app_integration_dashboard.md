# Owner App Integration Dashboard

Date: 2026-07-02 (F5-02)

## The picture in one look

| Question | Answer |
|---|---|
| Did anything connect to the database? | No. The app remains fully disconnected (re-verified: zero references) |
| What did F5-02 produce? | The complete rulebook for the future connection: 9 contract specs, 6 machine contracts, 57 mock test scenarios, stop conditions, and the phase plan |
| What is the next step? | F5-03: a design-only review that turns the rulebook into a file-level build plan. No code changes |
| When does code get written? | F5-04, only after you approve the F5-03 design (O7-design), on a feature branch, against a mock backend, still no database |
| When does the app first touch the preview database? | F5-05, only after the reload (O6), tier promotion (O2), and your build approval (O7) |

## Signatures on your desk (new this pass)

| ID | Decision | Recommendation |
|---|---|---|
| P1 | Ratify the contract set | Ratify |
| P2 | Approve F5-03 design phase | Approve (fits the Fable window) |
| P3/P4 | Two technical mechanism choices | Delegate to F5-03 recommendation |
| P5 | QA-mode excerpt flag, default off | Approve |
| P6 | Refusal template review | Schedule one sitting |

O1 (start the corpus-admin review) from the previous packet remains the most time-critical signature overall.

## What the contracts guarantee (plain language)

Every legal statement will carry a database-verified citation or the system refuses; nothing outside Titles 36/37, TRJPP, TRE (evidence questions only), and labeled DCS reference cards can enter an answer; restricted and unreviewed material cannot render anywhere; every answer and refusal is reconstructable from audit rows; the QA build wears a permanent "not for judicial reliance" banner; and no configuration can point at production.
